pipeline {
	agent any
	stages {
		stage ('sonar') {
			steps {
				withSonarQubeEnv('SonarPipeline'){
					withMaven(maven: 'My-Maven'){
						sh 'clean install sonar:sonar'
					}
				}
			}

		}
	}
}
